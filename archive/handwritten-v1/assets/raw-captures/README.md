Raw source captures for Heaven Fall cards belong in this directory.

Use one flat, tightly cropped image per card. The build script reads from this
directory by default and publishes cleaned copies into `docs/assets/source-cards/`.
